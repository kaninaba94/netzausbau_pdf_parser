

To run streamlit app to extract raw table data from PDFs: 
1. Install dependencies in a python virtual environment, e.g. using `poetry install --no-root` in repo root
2. `streamlit run streamlit_tableparser.py -- --pdf-path ...`
