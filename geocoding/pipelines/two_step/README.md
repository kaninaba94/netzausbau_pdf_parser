
The current idea is to use a two-step, somewhat rather hard-coded pipeline to map raw measures to OSM features, and then to use a cleaned, corrected subset of those predictions as a dataset to train a dual-encoder ML model for a single-step pipeline.
